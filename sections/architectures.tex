%!TEX root = slides.tex

\section{Web Application Architectures}

\begin{frame}{What is a web application?}
  \begin{description}
    \item[Web applications] are web pages that are interactive, thanks to JavaScript.
    \item[JavaScript] is used to capture user events, and typically responds to them by firing off HTTP Requests in the background and changing the displayed resource based on the HTTP Response.
    \item[Resources] are typically generated on the fly by a web server, which usually interacts with a database.
    \item[LAMP] is an older architecture stack that follows this pattern.
    \item[MEAN] is a more modern stack.
  \end{description}
\end{frame}

\begin{frame}{Considerations}
  \begin{description}
    \item[Discoverable] Identifiable as applications, findable by search engines.
    \item[Linkable] Easily shareable via URLs, without requiring complex installation.
    \item[Progressive] Works for every user, regardless of browser choice.
    \item[Responsive] Fits any form factor: desktop, mobile, tablet, or whatever comes next.
    \item[Network] Works offline or on low quality networks.
  \end{description}
  \citeurl{developer.mozilla.org/en-US/Apps/Progressive}
\end{frame}

\begin{frame}{HTTP and CRUD}
  \tikzstyle{rect} = [rectangle,fill=gmitblue,text width=5em,text centered,minimum height=16em,rounded corners,text=white]
  \begin{adjustbox}{max width={0.9\textwidth},center} 
    \begin{tikzpicture}[thick]

      \node [rect] (database) at (10,0) {Database};
      \node [rect] (webserver) at (5,0) {Web Server};
      \node [rect] (client) at (0,0) {Client};

      \path ([yshift=5.8em]webserver.west) edge[->]                                             ([yshift=5.8em]client.east);
      \path ([yshift=6.2em]webserver.west) edge[<-] node[anchor=south] {\footnotesize POST}     ([yshift=6.2em]client.east);
      
      \path ([yshift=5.8em]database.west) edge[->]                                              ([yshift=5.8em]webserver.east);
      \path ([yshift=6.2em]database.west) edge[<-] node[anchor=south] {\footnotesize CREATE}    ([yshift=6.2em]webserver.east);

      \path ([yshift=1.8em]webserver.west) edge[->]                                             ([yshift=1.8em]client.east);
      \path ([yshift=2.2em]webserver.west) edge[<-] node[anchor=south] {\footnotesize GET}      ([yshift=2.2em]client.east);
      
      \path ([yshift=1.8em]database.west) edge[->]                                              ([yshift=1.8em]webserver.east);
      \path ([yshift=2.2em]database.west) edge[<-] node[anchor=south] {\footnotesize RETRIEVE}  ([yshift=2.2em]webserver.east);

      \path ([yshift=-2.2em]webserver.west) edge[->]                                            ([yshift=-2.2em]client.east);
      \path ([yshift=-1.8em]webserver.west) edge[<-] node[anchor=south] {\footnotesize PUT}     ([yshift=-1.8em]client.east);
      
      \path ([yshift=-2.2em]database.west) edge[->]                                             ([yshift=-2.2em]webserver.east);
      \path ([yshift=-1.8em]database.west) edge[<-] node[anchor=south] {\footnotesize UPDATE}   ([yshift=-1.8em]webserver.east);

      \path ([yshift=-6.2em]webserver.west) edge[->]                                            ([yshift=-6.2em]client.east);
      \path ([yshift=-5.8em]webserver.west) edge[<-] node[anchor=south] {\footnotesize DELETE}  ([yshift=-5.8em]client.east);
      
      \path ([yshift=-6.2em]database.west) edge[->]                                             ([yshift=-6.2em]webserver.east);
      \path ([yshift=-5.8em]database.west) edge[<-] node[anchor=south] {\footnotesize DELETE}   ([yshift=-5.8em]webserver.east);

      \draw ([yshift=6.2em]webserver.east) edge[dashed,draw=gray!60] ([yshift=6.2em]webserver.west);
      \draw ([yshift=5.8em]webserver.east) edge[dashed,draw=gray!60] ([yshift=5.8em]webserver.west);
      \draw ([yshift=2.2em]webserver.east) edge[dashed,draw=gray!60] ([yshift=2.2em]webserver.west);
      \draw ([yshift=1.8em]webserver.east) edge[dashed,draw=gray!60] ([yshift=1.8em]webserver.west);
      \draw ([yshift=-2.2em]webserver.east) edge[dashed,draw=gray!60] ([yshift=-2.2em]webserver.west);
      \draw ([yshift=-1.8em]webserver.east) edge[dashed,draw=gray!60] ([yshift=-1.8em]webserver.west);
      \draw ([yshift=-6.2em]webserver.east) edge[dashed,draw=gray!60] ([yshift=-6.2em]webserver.west);
      \draw ([yshift=-5.8em]webserver.east) edge[dashed,draw=gray!60] ([yshift=-5.8em]webserver.west);

    \end{tikzpicture}
  \end{adjustbox}
\end{frame}